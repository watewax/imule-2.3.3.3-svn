# Configuration for /etc/init.d/imule-daemon

# The init.d script will only run if this variable non-empty.
IMULED_USER=""

# You can set this variable to make the daemon use an alternative HOME.
# The daemon will use $IMULED_HOME/.iMule as the directory, so if you
# want to have $IMULED_HOME the real root (with an Incoming and Temp
# directories), you can do `ln -s . $IMULED_HOME/.iMule`.
IMULED_HOME=""
